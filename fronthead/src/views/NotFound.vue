<template>
  <!-- 404页面根容器：全屏弹性垂直居中布局，白色背景保证简洁视觉，适配不同屏幕高度 -->
  <div class="not-found-container">
    <!-- 图片容器：相对定位（为悬浮表情提供绝对定位参考），inline-block适配图片宽度 -->
    <div class="img-wrapper">
      <img 
        class="not-found-img" 
        src="@assets/images/404.png" 
        alt="404页面不存在" 
        loading="lazy"
      >
      <!-- 悬浮表情：初始隐藏，hover图片时显示并触发弹跳动画，增加页面趣味交互 -->
      <div class="fail-emoji">😅</div>
    </div>
    
    <!-- 404标题：口语化文案降低用户焦虑，大号字体突出核心提示 -->
    <h1 class="not-found-title">页面走丢了~</h1>
    <!-- 404描述文本：清晰说明页面不存在的可能原因，浅灰色弱化视觉冲击 -->
    <p class="not-found-desc">
      抱歉，你访问的页面不存在、已被删除或路径错误
    </p>
    
    <!-- 操作按钮区：提供三种核心操作（返回根路径/主页/刷新），弹性布局适配不同屏幕 -->
    <div class="not-found-actions">
      <button @click="goStart" class="btn start-btn">开始</button>
      <button @click="goHome" class="btn home-btn">返回主页</button>
      <button @click="refreshPage" class="btn refresh-btn">刷新页面</button>
    </div>
    
    <!-- 辅助提示文本：补充引导用户检查网址，小号浅灰色作为次要信息 -->
    <p class="not-found-tip">
      检查网址是否正确，或返回主页继续浏览
    </p>
  </div>
</template>

<script setup>
/**
 * 404页面脚本逻辑
 * @description 提供页面不存在时的核心操作：返回网站根路径、返回应用主页、刷新当前页面，通过编程式导航实现路由跳转
 * @dependencies vue-router: 实现路由跳转（返回根路径/主页）
 */
import { useRouter } from 'vue-router'

/** 
 * Vue Router路由跳转实例（编程式导航）
 * @type {import('vue-router').Router} 
 * 用途：实现404页面的路由跳转（返回根路径/应用主页）
 */
const router = useRouter()

/**
 * 跳转至网站根路径
 * @description 点击「开始」按钮触发，跳转至网站最顶层路径（/），适用于用户重新开始浏览
 * @returns {Promise<void>} 路由跳转异步Promise
 */
const goStart = () => {
  router.push('/')
}

/**
 * 跳转至应用主页
 * @description 点击「返回主页」按钮触发，跳转至系统核心首页（/app/home），引导用户回到可用页面
 * @returns {Promise<void>} 路由跳转异步Promise
 */
const goHome = () => {
  router.push('/app/home')
}

/**
 * 刷新当前页面
 * @description 点击「刷新页面」按钮触发，通过原生API刷新当前页面，解决临时路径加载异常问题
 * @returns {void}
 */
const refreshPage = () => {
  window.location.reload()
}
</script>

<style scoped>
/* 
 * 404页面样式总说明：
 * 1. 设计原则：友好化（口语化文案）+ 趣味交互（悬浮表情弹跳）+ 引导性（多操作按钮）+ 响应式适配
 * 2. 视觉风格：简洁白色背景+渐变按钮+趣味表情，降低404错误的负面体验
 * 3. 交互体验：图片hover放大+表情弹跳动画+按钮hover上浮/阴影加深，提升操作反馈
 * 4. 响应式：768px以下适配移动端（图片缩小、按钮垂直布局、字号调整）
 * 5. 性能优化：图片懒加载，动画过渡流畅不卡顿
 */

/* 404页面根容器：最小高度100vh占满屏幕 + 弹性垂直居中 + 内边距适配小屏 + 文字居中 */
.not-found-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  padding: 2rem;
  text-align: center;
  background-color: #ffffff; /* 白色背景，避免视觉杂乱 */
  color: #2d3748; /* 主文字深灰色，保证可读性 */
}

/* 图片容器：相对定位（为表情提供绝对定位参考） + inline-block适配图片宽度，避免占满整行 */
.img-wrapper {
  position: relative;
  display: inline-block;
}

/* 404图片：响应式宽度+自动高度+透明度过渡+懒加载，保证不同屏幕下比例正常 */
.not-found-img {
  width: 800px; /* 大屏下的最优展示宽度 */
  height: auto; /* 保持图片原始比例 */
  margin: 0;
  opacity: 0.9; /* 轻微透明，弱化图片视觉占比 */
  transition: all 0.3s ease; /* 过渡动画，hover时状态切换流畅 */
}

/* 失败表情：初始隐藏 + 绝对定位（图片右上角） + 大字号 + 弹性过渡 + 屏蔽鼠标事件（避免遮挡图片hover） */
.fail-emoji {
  position: absolute;
  top: -50px; /* 图片上方偏移，避免遮挡图片主体 */
  right: 50px; /* 图片右侧偏移，平衡视觉 */
  font-size: 5rem; /* 大字号强化趣味效果 */
  opacity: 0; /* 初始隐藏 */
  transform: scale(0.5) translateY(20px); /* 初始缩小+下移，为弹跳动画做铺垫 */
  transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275); /* 弹性过渡，动画更自然 */
  pointer-events: none; /* 屏蔽鼠标事件，避免影响图片hover触发 */
}

/* hover图片容器时：显示表情+触发弹跳动画，增加趣味交互 */
.img-wrapper:hover .fail-emoji {
  opacity: 1; /* 显示表情 */
  transform: scale(1) translateY(0); /* 恢复正常大小和位置 */
  animation: bounce 0.6s ease; /* 触发弹跳动画 */
}

/* hover图片容器时：图片轻微放大+完全不透明，强化交互反馈 */
.img-wrapper:hover .not-found-img {
  opacity: 1; /* 完全不透明，突出图片 */
  transform: scale(1.02); /* 轻微放大，提升视觉感知 */
}

/* 弹跳动画：带轻微旋转的弹性弹出效果，模拟表情"跳出来"的趣味感 */
@keyframes bounce {
  0% { transform: scale(0.5) translateY(20px) rotate(-10deg); } /* 初始状态：缩小+下移+左旋转 */
  60% { transform: scale(1.1) translateY(-5px) rotate(5deg); } /* 中间状态：放大+上移+右旋转，形成弹性 */
  100% { transform: scale(1) translateY(0) rotate(0); } /* 最终状态：恢复正常 */
}

/* 404标题：大号字体+底部间距+主色调，突出核心提示，口语化文案降低用户焦虑 */
.not-found-title {
  font-size: 2rem;
  margin: 1rem 0;
  color: #2d3748;
}

/* 描述文本：适中字号+浅灰色+宽度限制+行高优化，清晰说明原因且不刺眼 */
.not-found-desc {
  font-size: 1.1rem;
  color: #718096; /* 浅灰色，弱化次要信息 */
  max-width: 500px; /* 限制宽度，避免文字过长影响阅读 */
  margin-bottom: 2rem;
  line-height: 1.6; /* 优化行高，提升可读性 */
}

/* 按钮容器：弹性布局+自动换行+居中+间距，适配不同屏幕宽度，保证按钮排列美观 */
.not-found-actions {
  display: flex;
  gap: 1rem; /* 按钮间水平间距 */
  margin-bottom: 2rem;
  flex-wrap: wrap; /* 小屏自动换行 */
  justify-content: center; /* 按钮居中排列 */
}

/* 通用按钮：统一内边距+圆角+无边界+字号+文字白色+过渡动画，保证按钮样式一致性 */
.btn {
  padding: 0.8rem 2rem;
  border-radius: 8px; /* 圆角提升现代感 */
  border: none; /* 清除默认边框 */
  font-size: 1rem;
  font-weight: 500; /* 字重提升可读性 */
  cursor: pointer;
  transition: all 0.2s ease; /* 过渡动画，hover/active状态切换流畅 */
  color: white; /* 文字白色，保证渐变背景下的可读性 */
}

/* 开始按钮：紫蓝渐变+阴影+hover/active效果，区分不同操作按钮 */
.start-btn {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); /* 紫蓝渐变，视觉区分 */
  box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3); /* 轻微阴影，提升层次感 */
}
/* 开始按钮hover：深色渐变+上浮+阴影加深，强化交互反馈 */
.start-btn:hover {
  background: linear-gradient(135deg, #5a6edb 0%, #6b449a 100%);
  transform: translateY(-3px); /* 轻微上浮，突出可点击 */
  box-shadow: 0 6px 16px rgba(102, 126, 234, 0.4);
}
/* 开始按钮active：轻微下移，模拟按压效果 */
.start-btn:active {
  transform: translateY(-1px);
}

/* 返回主页按钮：蓝青渐变+阴影+hover/active效果，与开始按钮视觉区分 */
.home-btn {
  background: linear-gradient(135deg, #3498db 0%, #2ecc71 100%);
  box-shadow: 0 4px 12px rgba(52, 152, 219, 0.3);
}
.home-btn:hover {
  background: linear-gradient(135deg, #2980b9 0%, #27ae60 100%);
  transform: translateY(-3px);
  box-shadow: 0 6px 16px rgba(52, 152, 219, 0.4);
}
.home-btn:active {
  transform: translateY(-1px);
}

/* 刷新按钮：橙黄渐变+阴影+hover/active效果，区分核心操作 */
.refresh-btn {
  background: linear-gradient(135deg, #f7971e 0%, #ffd200 100%);
  box-shadow: 0 4px 12px rgba(247, 151, 30, 0.3);
}
.refresh-btn:hover {
  background: linear-gradient(135deg, #f57c00 0%, #fbc02d 100%);
  transform: translateY(-3px);
  box-shadow: 0 6px 16px rgba(247, 151, 30, 0.4);
}
.refresh-btn:active {
  transform: translateY(-1px);
}

/* 辅助提示文本：小号字体+浅灰色，作为次要信息，不抢占视觉焦点 */
.not-found-tip {
  font-size: 0.9rem;
  color: #a0aec0; /* 更浅的灰色，弱化次要信息 */
}

/* 移动端适配（768px以下，平板/手机）：优化小屏展示效果 */
@media (max-width: 768px) {
  /* 404图片缩小，适配小屏宽度 */
  .not-found-img {
    width: 200px; /* 移动端最优展示宽度 */
  }
  
  /* 表情大小/位置适配移动端，避免超出屏幕 */
  .fail-emoji {
    font-size: 3rem; /* 缩小表情字号 */
    top: -30px; /* 减少上方偏移 */
    right: 20px; /* 减少右侧偏移 */
  }
  
  /* 标题字号缩小，适配小屏视觉 */
  .not-found-title {
    font-size: 1.5rem;
  }
  
  /* 按钮容器改为垂直布局+宽度限制，适配小屏触控操作 */
  .not-found-actions {
    flex-direction: column; /* 垂直排列，避免横向挤压 */
    width: 100%;
    max-width: 300px; /* 限制最大宽度，避免按钮过宽 */
    gap: 0.8rem; /* 减少垂直间距，节省屏幕空间 */
  }
  
  /* 按钮宽度100%+内边距调整，提升触控热区 */
  .btn {
    width: 100%; /* 占满容器宽度，提升点击面积 */
    padding: 0.9rem 0; /* 调整内边距，保证按钮高度适中 */
  }
}
</style>